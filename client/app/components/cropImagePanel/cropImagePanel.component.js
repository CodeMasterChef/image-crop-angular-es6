import template from './cropImagePanel.html';
import controller from './cropImagePanel.controller';
import './cropImagePanel.scss';

let cropImagePanelComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default cropImagePanelComponent;
